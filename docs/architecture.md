# 📜 SAO ENGINE - ARCHITECTURE DOCUMENTATION
Generated by Hermes v10

## 🚀 Core Pillars (Shared)
### Cardinal (Data Engine)
- **Path:** `ReplicatedStorage.Cardinal`
- **Role:** Central Hub. Links Data, Events, and all other Pillars.

### Atlas (Asset Intelligence)
- **Path:** `ReplicatedStorage.Atlas`
- **Role:** Handles fetching, importing, analyzing, and integrating assets from GitHub.
- **Dependencies:** Workspace

### Helios (World Logic)
- **Path:** `ReplicatedStorage.Helios`
- **Role:** Manages zones, world building logic, and environment states.

### Titan (Combat Engine)
- **Path:** `ReplicatedStorage.Titan`
- **Role:** Manages combat state machines, combos, skills, and AI behaviors.
- **Dependencies:** Profiles, StateMachine

### Aurora (UI Engine)
- **Path:** `ReplicatedStorage.Aurora`
- **Role:** Visual feedback, HUDs, menus, and client-side rendering.

### Nexus (Network & Telemetry)
- **Path:** `ReplicatedStorage.Nexus`
- **Role:** Balances the game based on real-time analytics and handles networking.

### Orion (Asset Management)
- **Path:** `ReplicatedStorage.Orion`
- **Role:** Organizes items, weapons, combos, and hitboxes.
- **Dependencies:** Registry, ComboSystem

## 🛠️ Server Services (ServerScriptService)
- **DataService** (`Cardinal`): Uses ProfileService and ReplicaService for persistent player data.
- **HeliosService** (`Helios`): Builds the physical world zones on startup.
- **SpawnerService** (`Helios`): Handles automated mob spawning based on zone profiles.
- **AssetService** (`Orion`): Welds and equips weapons to players.
- **SkillEngine** (`Titan`): Executes player skills and calculates systemic damage via RaycastHitbox.
- **AIService** (`Titan`): Drives mob logic, aggro, and pathfinding.
- **TelemetryService** (`Nexus`): Records usage stats and zone visits.
- **BalancingEngine** (`Nexus`): Auto-adjusts skill multipliers based on telemetry.
- **LibraService** (`Libra`): The central brick cache. Loads all configuration modules on boot.
- **AtlasEngine** (`Atlas`): Server-side handler for external asset requests.
- **GaiaService** (`Gaia`): The Procedural World Creator. Builds forests, towns, and dungeons algorithmically.
- **OracleService** (`Oracle`): Predicts server load and player behavior.
- **PrometheusService** (`Prometheus`): The Dynamic Optimizer. Adjusts FX, AI, and Network rates to save FPS/Memory.
- **SentinelService** (`Sentinel`): The System Guardian. Monitors errors, infinite yields, and auto-fixes corrupted elements.

## 🎮 Client Controllers (StarterPlayerScripts)
- **CombatController** (`Aurora`): Manages the player's State Machine (Idle, Dodging, Attacking) and Input Buffering (Combos).
- **HUDController** (`Aurora`): Syncs health and current zone visuals via Replica.
- **InventoryController** (`Aurora`): Renders the player's items dynamically.
- **AtmosphereController** (`Aurora`): Tweaks Lighting and Atmosphere based on the current Helios Zone.
- **AdminController** (`Aurora`): Hidden developer panel (Key: P).
- **VisualFeedbackController** (`Aurora`): Spawns damage popups and level-up notifications.
- **MenuController** (`Aurora`): The SAO main menu (Key: M or Esc).
- **SoundController** (`Aurora`): SafePlay wrapper for dynamic BGM and SFX.
- **WorldController** (`Aurora`): Updates zone notifications dynamically.

---
*Document automatically generated by Hermes v10.*